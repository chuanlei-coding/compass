# Word Add-ins 前端运行日志查看完整指南

## 概述

Word Add-ins 的前端代码运行在 Office 应用内置的浏览器环境中。所有前端日志（console.log、错误等）都会显示在浏览器开发者工具中。

## 关键概念

### Word Add-ins 的运行环境

```
Word应用
    └── 内置浏览器（基于Edge/IE）
        └── 加载插件HTML/JS
            └── 执行前端代码
                └── 日志输出到浏览器控制台
```

**重要：**
- 前端代码在**Word内置的浏览器**中运行
- 不是系统浏览器（Chrome、Firefox等）
- 日志在Word的开发者工具中查看

## 查看日志的方法

### 方法1：使用F12开发者工具（最常用）

#### 步骤详解

1. **打开Word并加载插件**
   ```
   - 打开Microsoft Word
   - 加载插件（通过"插入" > "我的加载项"）
   - 打开插件侧边栏
   ```

2. **打开开发者工具**
   
   **方式A：快捷键（推荐）**
   - 在Word中按 **F12** 键
   - 开发者工具窗口会弹出

   **方式B：右键菜单**
   - 右键点击插件侧边栏区域
   - 选择"检查"或"Inspect"或"检查元素"
   - 开发者工具会打开

   **方式C：通过Word选项**
   - 文件 > 选项 > 高级
   - 勾选"显示加载项用户界面错误"
   - 但这只会显示错误弹窗，不是完整控制台

3. **查看Console标签页**
   - 开发者工具打开后，默认可能显示其他标签页
   - 点击 **Console**（控制台）标签页
   - 所有前端日志都会显示在这里

#### 不同Word版本的差异

**Word 2016/2019（桌面版）：**
- 使用IE/Edge内核
- F12打开IE开发者工具
- 界面可能看起来较旧

**Word 365（现代版）：**
- 使用Edge Chromium内核
- F12打开Edge开发者工具
- 界面类似Chrome DevTools

**Word Online：**
- 在浏览器中运行
- 使用浏览器的开发者工具（F12）
- 与普通网页开发相同

### 方法2：查看Network标签页

查看网络请求日志：

1. 打开开发者工具（F12）
2. 切换到 **Network**（网络）标签页
3. 执行操作（如发送消息）
4. 查看请求：
   - `taskpane.html` - 插件主页面
   - `taskpane.js` - 前端JavaScript代码
   - `api/process` - 后端API调用
   - 其他资源文件

**查看请求详情：**
- 点击请求查看详情
- Headers：请求头和响应头
- Preview/Response：响应内容
- Timing：请求时间线

### 方法3：查看Sources标签页

查看源代码和断点调试：

1. 打开开发者工具（F12）
2. 切换到 **Sources**（源代码）标签页
3. 可以：
   - 查看加载的JavaScript文件
   - 设置断点进行调试
   - 单步执行代码

### 方法4：查看Application标签页

查看存储和缓存：

1. 打开开发者工具（F12）
2. 切换到 **Application**（应用）标签页
3. 可以查看：
   - Local Storage：本地存储的数据
   - Session Storage：会话存储
   - Cookies：Cookie数据

**检查配置：**
```javascript
// 在Console中执行
localStorage.getItem('ai_api_key')
localStorage.getItem('backend_url')
```

## 日志类型和位置

### 1. Console日志（主要位置）

**所有 `console.log()` 输出都在这里：**

```
Console标签页
├── 普通日志（console.log）
├── 警告（console.warn）
├── 错误（console.error）
└── 信息（console.info）
```

**示例日志：**
```
检测到平台: Microsoft Word
Office.onReady 回调执行 {host: "Word", platform: "PC"}
运行平台: Microsoft Word
📄 获取文档内容...
文档内容长度: 123
🤖 调用AI服务处理请求: 你好
🚀 开始调用后端API...
📡 发送请求到后端API: http://localhost:8000/api/process
后端API响应状态: 200 OK
✅ 后端API调用成功
✏️ 执行编辑操作: 1 个操作
```

### 2. 错误日志

**JavaScript错误：**
- 红色错误信息
- 包含错误堆栈
- 显示文件名和行号

**示例：**
```
❌ React错误边界捕获到错误: TypeError: Cannot read property 'text' of undefined
    at ChatWindow.handleSend (taskpane.js:123:45)
    at onClick (taskpane.js:456:78)
```

### 3. 网络请求日志

**Network标签页显示：**
- 请求URL
- 请求方法（GET、POST等）
- 状态码（200、404、500等）
- 响应时间
- 请求/响应内容

### 4. React错误边界日志

**如果使用了ErrorBoundary：**
- React组件错误会显示在Console
- 同时可能显示错误边界UI

## 实际操作步骤

### 完整流程示例

1. **启动服务**
   ```bash
   # 终端1：启动后端
   cd backend
   uvicorn main:app --reload --port 8000
   
   # 终端2：启动前端
   npm run dev
   ```

2. **在Word中加载插件**
   - 打开Word
   - 插入 > 我的加载项 > 加载插件

3. **打开开发者工具**
   - 在Word中按 **F12**
   - 切换到 **Console** 标签页

4. **执行操作并查看日志**
   - 在插件中输入消息并发送
   - 观察Console中的日志输出
   - 查看Network标签页的网络请求

## 日志过滤和搜索

### 在Console中过滤

**按日志级别：**
- 点击Console顶部的图标过滤：
  - 错误（红色）
  - 警告（黄色）
  - 信息（蓝色）
  - 日志（灰色）

**按文本搜索：**
- 在Console顶部的搜索框输入关键词
- 例如：输入"API"只显示API相关日志
- 例如：输入"错误"只显示错误日志

**按日志来源：**
- 可以按文件过滤
- 可以按日志类型过滤

### 清除日志

- 点击Console左上角的清除按钮（🚫图标）
- 或按快捷键：
  - Windows: `Ctrl+L`
  - Mac: `Cmd+K`

## 常见日志位置问题

### 问题1：按F12没有反应

**可能原因：**
- Word版本不支持开发者工具
- 快捷键被其他程序占用
- 需要管理员权限

**解决方案：**
1. 尝试右键点击插件区域，选择"检查"
2. 检查Word版本（需要Word 2016+）
3. 尝试其他快捷键组合

### 问题2：开发者工具打开了但看不到日志

**可能原因：**
- 在错误的标签页
- 日志被过滤
- 插件未正确加载

**解决方案：**
1. 确保在 **Console** 标签页
2. 清除所有过滤器
3. 刷新插件（关闭并重新打开侧边栏）
4. 检查插件是否已加载（查看Network标签页是否有请求）

### 问题3：日志显示在系统浏览器而不是Word中

**错误做法：**
- 在Chrome/Firefox中打开 `https://localhost:3000/taskpane.html`
- 这样看不到Word Add-in的真实日志

**正确做法：**
- 必须在Word中打开插件
- 在Word中按F12打开开发者工具

### 问题4：看不到网络请求

**检查：**
1. 确保在 **Network** 标签页
2. 确保没有启用"保留日志"（Preserve log）
3. 执行操作后再查看
4. 检查请求是否被过滤

## 调试技巧

### 1. 使用断点

在Sources标签页中：
1. 找到要调试的JavaScript文件
2. 点击行号设置断点
3. 执行操作，代码会在断点处暂停
4. 可以查看变量值、单步执行等

### 2. 实时修改代码

在Console中可以直接执行JavaScript：
```javascript
// 检查配置
console.log(localStorage.getItem('ai_api_key'));

// 手动调用函数
AIService.processRequest('测试', '文档内容');

// 检查对象
console.log(window.Office);
```

### 3. 监控网络请求

在Network标签页：
- 勾选"保留日志"（Preserve log）保持请求记录
- 可以右键请求选择"复制为cURL"进行测试
- 可以查看请求/响应的完整内容

### 4. 查看元素

在Elements标签页：
- 可以查看DOM结构
- 可以修改样式实时预览
- 可以查看元素的事件监听器

## 日志输出位置总结

### 前端日志输出位置

| 日志类型 | 输出位置 | 查看方法 |
|---------|---------|---------|
| `console.log()` | Console标签页 | F12 > Console |
| `console.error()` | Console标签页（红色） | F12 > Console |
| `console.warn()` | Console标签页（黄色） | F12 > Console |
| JavaScript错误 | Console标签页 | F12 > Console |
| React错误 | Console标签页 | F12 > Console |
| 网络请求 | Network标签页 | F12 > Network |
| localStorage | Application标签页 | F12 > Application |
| 断点调试 | Sources标签页 | F12 > Sources |

### 后端日志输出位置

| 日志类型 | 输出位置 | 查看方法 |
|---------|---------|---------|
| Python print/logging | 终端/控制台 | 运行uvicorn的终端 |
| FastAPI日志 | 终端/控制台 | 运行uvicorn的终端 |
| API请求日志 | 终端/控制台 | 运行uvicorn的终端 |

## 完整示例：查看一次完整的请求日志

### 步骤1：打开所有相关工具

1. **Word中打开插件和开发者工具**
   - 在Word中加载插件
   - 按F12打开开发者工具
   - 切换到Console标签页

2. **打开后端服务终端**
   - 查看后端日志输出

3. **打开前端服务终端（可选）**
   - 查看webpack编译日志

### 步骤2：执行操作

在插件中输入："将第一段加粗"并发送

### 步骤3：查看日志

**Console标签页（前端）：**
```
检测到平台: Microsoft Word
📄 获取文档内容...
文档内容长度: 156
🤖 调用AI服务处理请求: 将第一段加粗
🚀 开始调用后端API...
📡 发送请求到后端API: http://localhost:8000/api/process
后端API响应状态: 200 OK
后端API响应数据: {message: "...", edits: [...]}
✅ 后端API调用成功
✏️ 执行编辑操作: 1 个操作
```

**Network标签页（前端）：**
```
POST http://localhost:8000/api/process
Status: 200 OK
Time: 1.2s
Request Headers: ...
Response: {...}
```

**后端终端（后端）：**
```
INFO:     127.0.0.1:xxxxx - "POST /api/process HTTP/1.1" 200 OK
调用AI API: https://api.openai.com/v1/chat/completions, 模型: gpt-3.5-turbo
成功处理请求: 将第一段加粗...
```

## 快速检查清单

✅ **前端日志查看：**
- [ ] 在Word中加载插件
- [ ] 按F12打开开发者工具
- [ ] 切换到Console标签页
- [ ] 执行操作
- [ ] 查看日志输出

✅ **网络请求查看：**
- [ ] 打开Network标签页
- [ ] 执行操作
- [ ] 查看API请求
- [ ] 检查请求状态和响应

✅ **后端日志查看：**
- [ ] 查看运行uvicorn的终端
- [ ] 查看API请求日志
- [ ] 查看错误信息（如果有）

## 总结

**Word Add-ins前端日志查看的关键：**

1. **位置**：Word内置浏览器的开发者工具（F12）
2. **主要标签页**：Console（日志）、Network（网络请求）
3. **前提**：必须在Word中运行插件，不能在系统浏览器中查看
4. **方法**：按F12或右键"检查"打开开发者工具

**记住：**
- ✅ 前端日志 → Word中的开发者工具（F12 > Console）
- ✅ 后端日志 → 运行uvicorn的终端
- ✅ 网络请求 → Word中的开发者工具（F12 > Network）

